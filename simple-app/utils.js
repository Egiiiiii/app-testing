// simple-app/utils.js

/**
 * Fungsi sederhana untuk mengalikan dua angka.
 * @param {number} a 
 * @param {number} b 
 * @returns {number}
 */
function multiply(a, b) {
    return a * b;
}

module.exports = {
    multiply,
};